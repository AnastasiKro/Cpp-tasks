#include <iterator>
#include <vector>
#include <iostream>
#include <lab4.h>
int main(){
	std::vector <tab> V;
	//V.push_back(5);
	//V.at(1)= 7;
	std::vector <tab> :: iterator it;
	//for (it = V.begin(); it<V.end(); it++)
		//std::cout<<*it;
	return 0;
}

